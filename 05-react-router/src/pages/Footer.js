import React from 'react';

// 화살표 함수를 사용해서 컴포넌트 생성

const Footer = () => {
  return (    // return 뒤에 괄호 사용안하려면 <div>가 return 옆에 있어야 한다. 아래로 내려가면 작동을 안함
    <div>
      <h1> Footer 블락입니다.</h1>
    </div>
  );
}

export default Footer;