function Footer() {   // 첫글자는 대문자

  return (
      <div>
        <hr/>   
        <h1> 여기는 Footer 블락입니다.</h1>
      </div>
  );
}
// // 태그블락이 있으면 반드시 닫아줘야 한다. 

export default Footer;   // 외부에서 가져다 사용하기 위해서는 export 해야한다. 

// 자바스크립트에서는 함수도 객체이다